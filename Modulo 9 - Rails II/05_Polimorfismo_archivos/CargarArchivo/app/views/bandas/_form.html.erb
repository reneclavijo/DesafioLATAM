<%= form_with(model: banda) do |form| %>
  <% if banda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banda.errors.count, "error") %> prohibited this banda from being saved:</h2>

      <ul>
        <% banda.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :logo %>
    <%= form.file_field :logo, accept: "image/png,image/jpg,image/jpeg" %>
  </div>

  <div class="field">
    <%= form.label :fotos %>
    <%= form.file_field :fotos, multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
