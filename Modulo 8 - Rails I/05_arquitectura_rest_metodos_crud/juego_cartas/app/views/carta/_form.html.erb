<%= form_with(model: cartum) do |form| %>
  <% if cartum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cartum.errors.count, "error") %> prohibited this cartum from being saved:</h2>

      <ul>
        <% cartum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
